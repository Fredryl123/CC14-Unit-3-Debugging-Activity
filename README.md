# CC14-Unit-3-Debugging-Activity
Debugging Activity – HTML & CSS Fixes
 HTML Fixes

Change name for link rel from "style.css" to "styles.css".

Closed missing </a> tag in the About navigation item.

Added missing closing </h2> in the About Us section.

Closed the <form> tag in the Contact section.

Removed inline styling from the navigation list.

 CSS Fixes

Added missing { after the body selector.

Corrected selector heade to header.

Moved nav text color styling from inline HTML into CSS.

Added missing closing } for main section.

Fixed footer errors (added missing semicolons and fixed text-align).